const config = {
  dev: true,
  clientID: 'client-id',
  clientSecret: 'client-secret',
}

// CHANGE FILE NAME TO "keys.js" AND FILL KEYS

// YOU ALSO NEED KEYS.JSON FROM CLOUD FIREBASE INSIDE CONFIG FOLDER

module.exports = config